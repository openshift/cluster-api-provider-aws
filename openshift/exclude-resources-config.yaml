apiVersion: v1
kind: ConfigMap
metadata:
  name: exclude-resources-config
  annotations:
    config.kubernetes.io/local-config: "true"
data:
  excludeCRDs: |
    awsmachinepools.infrastructure.cluster.x-k8s.io
  renameClusterRoles: |
    capa-manager-role=openshift-capa-manager-role
